selenium>=4.15.0
webdriver-manager>=4.0.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
PyQt6>=6.6.0

# Windows-only: criptografa a senha com DPAPI (não obrigatório; cai pra base64 se ausente)
pywin32>=306; sys_platform == "win32"

# Apenas para empacotar o .exe (não precisa em runtime):
# pip install pyinstaller
